# Theme Image Assets

Use these directories for theme-based images:

- `assets/images/light/`
- `assets/images/dark/`

Suggested filenames:

- `logo.png`
- `empty_state.png`

Both light and dark directories should keep the same filenames to make runtime mapping simple.
